# TakeSelfieBeta
the script allows you to tale a selfie using the camera of your PC and saves it to a certian path.

using CV2 Library
(don't forget to install the library before running the script)


